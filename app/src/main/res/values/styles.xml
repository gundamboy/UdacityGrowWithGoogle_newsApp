<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="android:textColorPrimary">@color/textColorPrimary</item>
        <item name="android:textColorSecondary">@color/textColorPrimary</item>
        <item name="android:textColor">@color/textColorPrimary</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:statusBarColor">@color/statusBarColor</item>
        <item name="android:windowLightStatusBar">true</item>
        <item name="android:windowBackground">@color/altWindowBackgroundColor</item>
        <item name="actionOverflowButtonStyle">@style/OverFlow</item>
    </style>

    <!--this is for the appbar-->
    <style name="OverFlow" parent="@style/AppTheme">
        <item name="android:src">@drawable/ic_action_navigation_more_vert</item>
        <item name="android:paddingEnd">@dimen/baseline_eight</item>
    </style>

    <style name="author_text" parent="@style/AppTheme">
        <item name="android:textColor">@color/captionText</item>
        <item name="android:textSize">@dimen/author_text_size</item>
    </style>

    <style name="section_date_text" parent="@style/AppTheme">
        <item name="android:textColor">@color/captionText</item>
        <item name="android:textSize">@dimen/section_date_text_size</item>
        <item name="android:textStyle">italic</item>
    </style>

    <style name="articleTitle" parent="@style/AppTheme">
        <item name="android:textColor">@color/textColorPrimary</item>
        <item name="android:textSize">@dimen/article_title_text</item>
    </style>

    <!--Preferences stuff-->
    <style name="SettingsFragmentTheme" parent="@style/AppTheme">
        <item name="android:dialogTheme">@style/PopUpDialog</item>
        <item name="android:alertDialogTheme">@style/PopUpDialog</item>
    </style>

    <!-- Settings Dialog Styles -->
    <style name="PopUpDialog" parent="@style/Theme.AppCompat.Dialog">
        <item name="android:textColorPrimary">@color/textColorPrimary</item>
        <item name="android:background">@color/colorPrimary</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

</resources>
